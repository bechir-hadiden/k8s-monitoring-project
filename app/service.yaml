apiVersion: v1
kind: Service
metadata:
  name: demo-app
  namespace: app
  labels:
    app: demo-app
spec:
  selector:
    app: demo-app
  ports:
    - name: http
      port: 80
      targetPort: 80
    - name: metrics
      port: 9113
      targetPort: 9113
  type: NodePort

